# AI Style Handoff — Faria Hossain

1. **Typography — Headings:** Set all headings (h1, h2, h3) in Cormorant Garamond, weight 300 (thin), all caps, with letter-spacing of 0.04em and a line-height of 1.1. Sizes: h1 `clamp(2rem, 5vw, 3.5rem)`, h2 `clamp(1.4rem, 3vw, 2rem)`, h3 `clamp(1.1rem, 2vw, 1.4rem)`.

2. **Typography — Body:** Set all body text, labels, and list content in Jost. Base size 1rem (16px), line-height 1.6.

3. **Color Palette:** Background is dark brown (`#2b1b10`). All text, headings, and links are off-white (`#f3ead9`). The moving background waves use a lighter brown (`#6b4a30`).

4. **Accent Usage:** Off-white is used for headings, body text, and every link. There are no filled buttons or solid borders anywhere on the page — the only "border" is the underline that appears under a link on hover.

5. **Hero Image:** The hero image runs full-width across the very top of the page and fills the entire first screen — minimum height 100vh on both laptop and phone — using `object-fit: cover`, with a dark gradient overlay fading from the bottom so text stays legible.

6. **Name & Contact Placement:** My name sits inside the hero image as the large heading. My email address sits directly beneath my name, inside the hero, styled as a plain off-white link.

7. **Layout — Columns:** Single column at every screen size. Content is centered with a max-width of 720px.

8. **Breakpoint:** One responsive breakpoint at 768px, used to adjust padding and spacing between laptop and phone.

9. **Work List — Link Style:** Every link in the Work list is plain text — no fill, no background, no border. Just the number and the title in off-white.

10. **Work List — Hover State:** On hover, an underline appears beneath the title with a smooth 0.25s ease transition. Nothing else about the link changes.

11. **Moving Background:** Three wave shapes drift sideways continuously at medium speed (13–16 second loops), spaced far apart across the page (top, middle, bottom) so each one reads as its own separate band rather than a cluster. They sit fixed behind all content and never interrupt reading.

12. **Consistency Rule:** Apply these typography, color, layout, and interaction rules identically across every future page or edit to this site, unless I give an explicit new instruction that overrides one of them.
